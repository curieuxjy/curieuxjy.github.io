---
draft: true
title: "📃Julia for robotics"
description: Simulation and real-time control in a high-level programming language
date: ""
categories: [julia, paper]
toc: true
---


[most-loved-dreaded-and-wanted-programming-scripting-and-markup-languages](https://survey.stackoverflow.co/2022/#section-most-loved-dreaded-and-wanted-programming-scripting-and-markup-languages)

# Abstract

‘two language problem’

requiring a low-level language for performance-sensitive components and a high-level language for interactivity and experimentation, which tends to increase software complexity

너무 공감되는데..

# Introduction

로봇은 다양한 일들을 하기를 요구 받기 때문에 소프트웨어에 요구되는 사항들이 많습니다.

예를 들면, 

oneline control이란 로봇이 동작을 하는 동안에 

마치 우리는 의식하고 있진 않지만 뇌와 몸의 신경계에서 매우 빠른 속도로 처리하는 것과 같은 일이 로봇에서는 oneline control로 이루어져야 하는 것이죠.

그래서 time budget을 넘으면 안됩니다.

물을 따르고 있는데 멈추라는 control이 너무 늦은 나머지 물이 컵에서 넘쳐 흐르면 안되는 것과 같이 

대부부은 millisecond 단위의 제어를 요구합니다. 매우 어렵죠

반대로 control을 하는 controller sytnesis는 offline에서 단 한번만 하게 되는데 완전 다른 데이터들을 계산하고 로봇의 움직임을 기호적으로 처리하게 됩니다.

<aside>
✅ controller synthesis is done just once, offline, but consumes the dynamics of the robot in symbolic form, requiring computation with an entirely different set of data types

</aside>

또한 로봇 프로그래밍 유저는 정말 다양합니다. 학생들부터 전문자들까지 그 범위가 정말 넓습니다.

대체적으로 빠르다고 생각하는 C/C++을 통해 lower level의 소프트웨어를 쓰게 되고 로봇의 동적인 모션이나 high level을 다룰때는 Python이나 Matlab을 쓰게 됩니다.(초보자나 프로토 타이핑에 적합한) - 예를 들면 Bullet이나 Drake, Tensorflow 같은 아이들이죠

근데 이렇게 two-lan을 쓰는 건 정말 단점이 많습니다.

1. 말 그대로 일을 2번하게 되는데 high에서 작성한 코드를 lower로 바꾸는 작업을 해야 합니다.
2. 여러 lan으로 작성된 프로그램은 유지 보수가 정말 어렵습니다.

그래서 줄리아는!!

파이썬처럼 인터렉티브하고 c처럼 퍼포먼스를 끌어올리기 위해 개발되었습니다@@

✅ In this paper, we provide evidence that supports this claim in the robotics domain. We present a number of Julia software packages written by the authors, and demonstrate that they can be used to implement a typical quadratic-programming (QP)based low-level balancing controller for the humanoid robot Atlas (e.g., [6], [7], [8], [9]) with low jitter in control frequency.


아래 패키지는 완전히 julia로만 써졌는데요!

- `RigidBodyDynamics.jl,` a pure-Julia rigid body dynamics library
- `MeshCat.jl`, a web-based, remotely controlled visualizer

![Untitled](https://s3-us-west-2.amazonaws.com/secure.notion-static.com/3bc280ec-f165-40e8-a77f-ed5fb89c9805/Untitled.png)

- `RigidBodySim.jl`, a simulation environment
- `Parametron.jl`, a framework for formulating and efficiently solving instances of a parameterized family of convex optimization problems
- `QPControl.jl`, an implementation of a QP-based lowlevel control algorithm for floating-base robots.

2장에서는 전체적인 줄리아 언어의 개요와 initial assessment

3장에서는 개발한 패키지 소개

4장에서는 휴머노이드 아틀라스에서 online control 예시를 통해 데모

5장에서는 마무리/결론 

# 2. Julia for Robotics

## A. Overview

- 2009년도부터 개발되어져온 언어
- 오픈소스
- numerical computing을 위한 목적으로 만들어짐
- LLVM이라는 걸 사용 → 뭔지 모르겟음
- garbage-collected이고 just-in-time이라는 (JIT) compiled를 사용
- 개발자들은 줄리아를 `optionally typed` 라고 부르는데, 데이터의 필드 타입이 생략되도되고 명시되어도 됨(단 명시되어 있으면 더 좋은 퍼포먼스를 보여주긴하지만)
- Julia has a rich type system that includes parametric types (similar to C++ template classes) and strong type inference capabilities that obviate the need to manually annotate the type of every variable (similar to C++’s auto keyword).
- 줄리아는 객체 지향형이 아니다
    - 기능들이 타입의 바깥에서 정의된다 - overloaded methods
    -


